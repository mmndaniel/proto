# PRD: bookmark-cli

A command-line bookmark manager. Save, list, and search bookmarks from the terminal.

## Core features
- Add bookmarks with URL, optional title, tags, and notes
- List all bookmarks with multiple output formats (text, table, JSON)
- Search bookmarks across all fields
- Recent bookmarks filter
